import Lesson1.Back_Quote_Test

fun main() {
    函数()

    // use back quote `` to call the special keyword function in kotlin with Java
    Back_Quote_Test().`in`()
    Back_Quote_Test().`is`()
}

// in kotlin, `` lets you can define the named function with other languages like Chinese
private fun `函数`() {
    println("This is a function")
}